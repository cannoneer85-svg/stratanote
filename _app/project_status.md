# Статус проекта и архитектура — Obsidian Collab

**Obsidian Collab** — это веб-приложение для совместной работы с базой знаний на основе Markdown. Проект сочетает визуальный опыт классического Obsidian (двусторонние вики-ссылки, граф связей, папки и файлы) с возможностями многопользовательского редактирования в реальном времени, контролем версий и администрированием.

---

## 1. Стек технологий и архитектура

Система разделена на три ключевых слоя:

### Клиент (Frontend)
Расположен в каталоге [_app/client](file:///d:/YandexDisk/AWG/md_obsidian/_app/client). Built на React + TypeScript + Vite.
* **Редактор**: построен на базе CodeMirror 6 с кастомными расширениями для подсветки синтаксиса Markdown и автодополнения вики-ссылок `[[заметка]]` при вводе.
* **Граф связей**: интерактивная 2D-визуализация сети документов на базе Force-directed graph (D3.js / HTML5 Canvas).
* **Стилизация**: CSS-переменные с поддержкой премиального темного режима, эффектов стекла (Glassmorphism), размытия фона и неонового свечения.

### Сервер (Backend)
Расположен в каталоге [_app/server](file:///d:/YandexDisk/AWG/md_obsidian/_app/server). Написан на Node.js + Express.
* **База данных**: SQLite (файл `database.sqlite` инициализируется через [db.js](file:///d:/YandexDisk/AWG/md_obsidian/_app/server/db.js)). Хранит пользователей, роли, историю версий файлов и активные блокировки.
* **Синхронизация в реальном времени**: Socket.io. Обеспечивает мгновенный обмен присутствием пользователей («кто онлайн и что редактирует»), уведомления об изменениях файлов и установку блокировок на редактирование.
* **Файловый наблюдатель (Watcher)**: библиотека Chokidar в [watcher.js](file:///d:/YandexDisk/AWG/md_obsidian/_app/server/watcher.js). Автоматически индексирует изменения, сделанные на диске внешними программами, обновляет БД SQLite и рассылает сокет-события клиентам.

### Облачная инфраструктура (Railway)
* **Деплой**: автоматическая сборка контейнера при пуше в ветку `master` на GitHub.
* **Персистентность**: подключен постоянный диск (Persistent Volume) в точку монтирования `/data` для сохранения базы данных SQLite (`/data/database.sqlite`) и каталога заметок (`/data/vault`) между деплоями и перезапусками контейнеров.

---

## 2. Ключевые архитектурные решения

* **Блокировка документов (Conflict Prevention)**: чтобы два редактора не перезаписали работу друг друга, при открытии вкладки в редакторе сервер накладывает эксклюзивный `Lock` на файл. Другие пользователи видят плашку «Редактирует: Имя пользователя» и могут просматривать документ только в режиме чтения (ReadOnly). Для предотвращения бесконечных циклов авто-блокировки/разблокировки в сокетах, клиент отфильтровывает собственную блокировку пользователя при передаче пропа `lockedBy` в редактор.
* **Маршрутизация по хэшу (Hash Routing)**: адресная строка браузера отражает путь к текущей открытой заметке в хэш-секции (например, `/#Общая архитектура проекта.md`). Приложение считывает этот хэш не только при кликах, но и при первоначальной загрузке страницы или ручном обновлении (F5), мгновенно открывая нужный документ и восстанавливая вкладки.
* **Двусторонние вики-ссылки (WikiLinks)**: ссылки вида `[[Имя заметки]]` или `[[Путь/Имя|Альтернативный текст]]` парсятся на сервере для построения связей графа и на клиенте для мгновенного перехода между заметками.
* **История версий (Revisions)**: при каждом сохранении файла на сервере контент записывается в таблицу `versions`. Клиенты могут открыть панель истории, выбрать любую прошлую версию, посмотреть визуальный Diff (разницу) в компоненте [DiffViewer.tsx](file:///d:/YandexDisk/AWG/md_obsidian/_app/client/src/components/DiffViewer.tsx) и восстановить её. Для корректности сравнения система сопоставляет выбранную версию N с её непосредственной предыдущей версией N-1 из базы данных (а не с текущей рабочей копией на диске), что позволяет точно видеть изменения, внесенные именно этой конкретной версией.
* **Устойчивость к сбоям (Self-Healing)**: сервер сверяет состояние диска и базы данных SQLite. При расхождениях (например, если файл удален с диска вручную) watcher автоматически удаляет запись и связанные версии из базы данных и синхронизирует списки у активных пользователей.

---

## 3. Реализованный функционал

### Управление заметками и проводник
* Создание, переименование и удаление папок и файлов `.md` прямо из сайдбара [Sidebar.tsx](file:///d:/YandexDisk/AWG/md_obsidian/_app/client/src/components/Sidebar.tsx).
* Дерево проводника поддерживает фокус на выбранной папке (для загрузок) и быстрый сброс на корень.
* **Интеллектуальный поиск по названию**: при вводе запроса проводник отбирает подходящие файлы и сохраняет структуру дерева (показывая родительские папки на всех уровнях), автоматически раскрывая их. Если совпадений нет, выводится надпись «Ничего не найдено».

### Парсер Markdown и визуализация (`Editor.tsx`)
Наш регулярный парсер в [Editor.tsx](file:///d:/YandexDisk/AWG/md_obsidian/_app/client/src/components/Editor.tsx) поддерживает:
* Заголовки `H1`-`H3`, жирный/курсивный текст, цитаты и ссылки.
* Списки задач (Checklists) в форматах `- [ ]` и `- [x]`.
* Маркированные списки (с маркерами `-`, `*`, `+`) и нумерованные списки (`1.`, `2.`).
* **Интеграция схем Mermaid**: автоматический парсинг блоков ` ```mermaid `, очистка HTML-сущностей (`&lt;`, `&gt;`), автоисправление опечаток стрелок (преобразование невалидных `<--->` в `<-->`). Рендеринг векторной графики в режиме просмотра управляется MutationObserver, который автоматически перерисовывает диаграммы при любых изменениях DOM со стороны React. Чтобы избежать сбоев при переключении вкладок и перерисовках, автоматический поиск `mermaid.run()` заменён на ручной вызов `mermaid.render(uniqueId, code)` с генерацией уникального ID на каждый проход рендеринга и непосредственной установкой SVG-кода в `innerHTML` с обязательной пометкой `data-processed="true"`, что предотвращает бесконечные циклы MutationObserver и гарантирует стабильную отрисовку схем.

### Панель администратора настроек (`SettingsPanel.tsx`)
Доступна только пользователям с ролью `Admin`. Состоит из двух вкладок:
1. **Импорт & Загрузка**:
   * Импорт ZIP-архива с выбором режима:
     * **Слияние** (по умолчанию): новые файлы добавляются, совпадающие обновляются (с записью новой версии в БД), лишнее не удаляется.
     * **Перезаписать все**: сервер удаляет все `.md` файлы и папки перед распаковкой архива (с предварительным confirm-предупреждением).
   * Загрузка одиночного `.md` документа в выбранную в сайдбаре директорию.
   * **Экспорт хранилища**: выгрузка всех заметок в ZIP-архив с динамическим названием по текущей дате и времени сервера: `obsidian-vault-export-YYYYMMDD-HHMM.zip`.
2. **Пользователи и Роли**:
   * Таблица всех пользователей с отображением статуса: «Активен» или «Ожидает одобрения».
   * **Система модерации (одобрения)**: пользователи, зарегистрировавшиеся самостоятельно на главной странице, не получают доступ к системе (при попытке входа выдается ошибка `403`), пока администратор не подтвердит их учетную запись нажатием кнопки-галочки «Одобрить».
   * **Запрет регистрации администраторов**: при саморегистрации роль `Admin` недоступна для выбора (и блокируется на стороне API). Создавать администраторов может только действующий администратор в панели настроек.
   * **Форма создания**: добавление новых пользователей администратором (создаются сразу активными).
   * **Inline-редактирование пользователей**: при нажатии на карандаш администратор может изменить имя пользователя, его роль, статус активности, а также задать новый пароль.
   * **Смена собственного пароля**: главный администратор может изменить себе пароль через режим редактирования. Поля изменения имени и роли для собственного аккаунта заблокированы в целях безопасности.
   * **Защита от самоблокировки**: администратор не может заблокировать, удалить или изменить роль собственного аккаунта.

---

## 4. Локальный запуск и тестирование

Для запуска и тестирования проекта на локальной машине выполните следующие шаги в терминале в корневой папке проекта (`d:\YandexDisk\AWG\md_obsidian`):

### Шаг 1. Установка зависимостей (выполняется один раз)
```bash
npm run install:all
```

### Шаг 2. Запуск проекта
Вы можете выбрать один из двух режимов запуска:

#### А. Запуск собранной production-версии (Рекомендуется)
Этот режим запускает бэкенд, который раздает скомпилированные и оптимизированные файлы фронтенда статически с порта `3001`:
1. Убедитесь, что проект собран:
   ```bash
   npm run build
   ```
2. Запустите сервер:
   ```bash
   npm start
   ```
3. Откройте в браузере: **[http://127.0.0.1:3001](http://127.0.0.1:3001)**

#### Б. Режим разработки (с горячей перезагрузкой кода)
В этом режиме фронтенд-сервер Vite запускается отдельно на порту `5173`, отслеживая изменения в реальном времени, а запросы к API проксируются на бэкенд (порт `3001`):
1. Запустите клиент и сервер одновременно:
   ```bash
   npm run dev
   ```
2. Откройте в браузере: **[http://localhost:5173](http://localhost:5173)**

